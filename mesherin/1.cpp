#include <iostream>
#include <numbers>


int sum(int a, int b) {
    return a + b;
}
int delimiter(int a, int b) {
    return a - b;
}

class People{
private:
    int age;
    int name;
public:


};
/*
int main() {
    using std::cout;
    using std::cin;
    using std::endl;

    /*
    int a, b;
    std::cin >> a >> b;
    std::cout << sum(a,b) << " " << delimiter(a,b) << std::endl;
    func(a,b);
    */

    /*
     * int x;
    cin >> x;
    cout << ++x << endl; // x = x + 1 (lvalue) x = 3 + 1
    cout << x++ << endl; // x + 1 (rvalue) 3 + 1
     */
    int x;
    cin >> x;

    if (int z = 9; x == 1) {
        cout << std::numbers::pi;

    } else if (x == 2) {
        cout << std::numbers::pi;
    }
}
*/
